spring:
  profiles:
    active: dev
  application:
    name: cyan-datagateway
  main:
    web-application-type: servlet
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
